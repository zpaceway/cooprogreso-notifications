{
  "name": "cooprogreso-pre-approved",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node ./build/index",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && cp -r ./databases ./build && cp -r ./templates ./build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.1.3",
    "dotenv": "^16.0.3",
    "handlebars": "^4.7.7",
    "qrcode-terminal": "^0.12.0",
    "read-excel-file": "^5.5.3",
    "whatsapp-web.js": "^1.18.0"
  },
  "devDependencies": {
    "@types/qrcode-terminal": "^0.12.0"
  }
}
